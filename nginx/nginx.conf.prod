server {
    listen 80;

    server_name demo.com;

    location / {
        proxy_pass http://frontend:3000;
    }

    location /api {
        proxy_pass http://backend:4000;
        rewrite ^/api/(.*) /$1 break;
    }
}